🐨 possible dependencies needed
📖 some commands used


FRONTEND
// material ui
🐨 npm install @material-ui/core

//Routing
🐨 npm install --save react-router-dom


BACKEND (Node.js)

//tracking the project's dependencies
📖 npm init -y

//initialize express.js
🐨 npm install --save express

//using nodemon to watch our app for changes and
//automatically reload files
🐨 npm install -g nodemon
📖 nodemon [server.js]

//docker
🐨 sudo apt install docker.io
📖 sudo usermod -aG docker $USER
📖 newgrp docker

//a complimentary tool for docker
🐨 docker-compose --version
📖 docker-compose up -d db

//node-pg-migrate (for proper migrations)
🐨 npm install --save node-pg-migrate pg
📖 npm run migrate up
📖 npm run migrate create name-table

//argon2
🐨 npm install --save argon2

//massive
🐨 npm install --save massive

//jwt
🐨 npm install --save jsonwebtoken